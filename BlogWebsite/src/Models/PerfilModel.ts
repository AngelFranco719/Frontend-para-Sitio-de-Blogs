export interface Perfil {
  id_Perfil: number | undefined;
  per_nombre: string;
  per_contraseña: string;
  per_descripcion: string;
  per_foto: string;
  per_portada: string;
  per_fecha: string;
}
